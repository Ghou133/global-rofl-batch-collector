# 安全报告

如果发现凭据泄露、可导致本地文件被错误覆盖/删除的漏洞，或认证信息被写入日志/产物，请通过 GitHub 仓库的 **Security → Report a vulnerability** 私下报告。若该入口尚未启用，请先联系维护者获取私下报告渠道；不要在公开 Issue 中粘贴真实凭据、lockfile、Replay 或原始玩家数据。

使用者应只用本人合法登录的客户端会话及自己的 Riot API Key。把 `.env`、`data/` 和 `logs/` 留在本地；公开诊断前先检查请求头、账号标识与完整比赛元数据。仓库的 `.gitignore` 不是上传前隐私审查的替代品。
