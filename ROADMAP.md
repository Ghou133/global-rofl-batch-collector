# 开发路线与进度

更新时间：2026-09-23。这里把**代码已实现**、**自动化已验证**和**真实客户端已验证**分开记录；不同平台或 patch 的结论不得互相代替。

## 目标与验收口径

本项目希望持续采集可追溯的 Replay：知道比赛与客户端版本、来源、文件 hash、失败原因，并能在中断后继续。国际服按平台和 patch 建立高分段单双排数据集；国服保留 Replay 与同局 SUMMARY/DETAILS 的配对证据。后续解析器可以使用这些数据，但语义解码不属于当前采集器的完成标准。

| 状态 | 判定方式 |
| --- | --- |
| 代码支持 | 平台映射、命令和存储分区存在，静态检查及单元测试通过 |
| 环境可用 | 指定平台和 patch 的 `probe` 在当前登录客户端通过 |
| 采集验收 | 真实下载、持久化、`status --verify-files` 和错误审查完成 |
| 语义可用 | 外部解析器对**精确 build** 的 packet/event 解码另行验收 |

## 已实现

| 模块 | 当前能力 | 验证范围 |
| --- | --- | --- |
| 统一入口 | `collector` / `collector.cmd` 分发国际服与 `cn` 子命令；旧 `kr_rofl_collector` 模块保留兼容入口 | 本地 CLI 测试 |
| 国际服发现 | 17 个可选平台，显式 League-V4、Match-V5、Data Dragon realm 映射；当前 patch、queue 420、官方排行榜筛选及候选质量排序 | 映射和服务单元测试；历史真实数据仅 KR |
| 国际服 Replay | 当前会话 Replay 路由 probe、下载重试、ReplayV2 容器校验、hash、原子发布、manifest、失败记录及恢复 | 自动化测试；KR patch 16.17 历史真实采集 108 个并全量完整性审计 |
| 国服配对 | 腾讯 HN1 会话的指定 game ID 下载、本机 Replay 扫描、基线/监听、SUMMARY/DETAILS 配对、状态查询和持久化 | 迁入源码测试与集成测试；不能据此声称其他国服平台可用 |
| 本地安全 | `.env`、`data/`、日志、Replay 与数据库不入库；会话凭据只用于运行时请求；数据根分离 | 静态检查和测试覆盖相关路径 |

## 未实现或未验收

1. **逐平台真实采集矩阵。** 对 17 个国际服平台分别记录测试日期、客户端登录平台、目标 patch、`probe` 结果、至少一条真实 Replay 的校验结果以及失败原因。现有 KR 历史结果不代表其他 16 区。
2. **腾讯国服其他平台。** `HN1` 之外的 SGP 配置与真实端点尚未完成验证；在获得可靠配置与合法会话前不宣布支持。
3. **国服自动发现新比赛。** `cn replay` 的无 ID 模式只扫描/处理已有候选；排行榜或比赛发现属于国服一般 `collect` 链，需要明确其与 Replay 队列的衔接和验收。
4. **Replay 语义解析。** 当前验证到容器与结构层。KR 旧样本的外部 parser 对 exact build `16.17.810.4348` 返回 `UNSUPPORTED_REPLAY_VERSION`，所以 packet/event 语义未通过。
5. **统一跨来源数据模型与导出。** 国际服和国服数据库有意独立；尚未提供跨来源查询、标准化导出或公开数据集。导出前需审查个人标识、来源约束及可再分发范围。
6. **更多运行环境。** Windows 和本地 League Client 是当前目标；macOS、Linux、无 GUI 服务运行未验收。

## 下一步顺序

### 近期：发布与可复现

- 在 GitHub CI 运行离线测试和 Ruff，公开每次提交的结果。
- 用干净的 Windows 环境按 README 重装，验证入口、配置和报错；记录 Python 版本与客户端环境。
- 建立按平台/patch 的验收表，附可脱敏的 probe、下载和完整性结果，不上传 Replay 或 token。

### 中期：采集范围

- 逐平台完成国际服真实采集矩阵，并处理区域特有的 realm、排行榜或 Replay 后端差异。
- 验证国服其他平台的 SGP 端点；将一般比赛发现和 Replay 配对队列衔接，但仍保留失败与原始证据。
- 为长时间运行加入明确的健康指标、容量预估和可恢复的限额控制。

### 后期：数据利用

- 定义经隐私审查的只读导出格式，并写清原始字段与标准化字段的映射。
- 如有精确 build 的外部 parser，再独立验收语义事件；每个 build 单独标记状态。
- 对保留策略、数据库迁移、跨版本兼容做带备份与失败回滚的设计和测试。

路线图是待办和验收条件，不保证日期；真实客户端与服务端状态会随 patch 变化。
